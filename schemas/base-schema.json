{
  "type": "object",
  "required": ["namespace", "region", "values", "applicationName"],
  "properties": {
    "namespace": {
      "type": "string",
      "examples": "perimeter"
    },
    "region": {
      "type": "string",
      "enum": ["sheker1", "sheker2", "sheker3", "try"],
      "examples": "sheker1"
    },
    "applicationName": {
      "type": "string",
      "examples": "tyk-prov-examples"
    }
  }
}
