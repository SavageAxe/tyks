{
  "type": "object",
  "required": ["namespace", "region", "values", "applicationName"],
  "properties": {
    "namespace": {
      "type": "string",
      "example": "perimeter"
    },
    "region": {
      "type": "string",
      "enum": ["sheker1", "sheker2", "sheker3", "try", "dev"],
      "example": "dev"
    },
    "applicationName": {
      "type": "string",
      "example": "tyk-prov-example-hey"
    }
  }
}
